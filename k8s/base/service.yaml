apiVersion: v1
kind: Service
metadata:
  name: gitops-demo-app
  labels:
    app: gitops-demo-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: gitops-demo-app
